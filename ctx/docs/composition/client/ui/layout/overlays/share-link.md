# Наложение «Ссылка для соединения»

Путь: `ctx/docs/composition/client/ui/layout/overlays/share-link.md`

## Назначение

Описывает визуальный слой, который активируется в `waiting` и плавает над трёхзонным скелетом, сохраняя зоны `action` и `Footer` без перестроений. Наложение подчёркивает, что `sessionId` открыт и ждёт второго участника, независимо от того, кто распределил ссылку, — поэтому оно остаётся нейтральным и не назначает роли.

## Компоновка

- Заголовок повторно использует `screen-header` с текстом «Ссылка для соединения» и размещает `header-action-button` в слоте действий для закрытия наложения.
- Основная зона выводит URL приглашения (`waiting-link`) и две `big-button`: одна инициирует шаринг, другая копирует ссылку; обе кнопки сохраняют единый CTA-стиль, при необходимости добавляя глифы через `icon-wrapper`.
- Footer-зона содержит короткий `screen-note`, объясняющий, как поделиться ссылкой, не дублируя тосты; подтверждения и ошибки показываются через `toast-layer`.

## Контракт адреса

- Ссылка генерируется как `https://<host>/?session=<uuid>` без дополнительных параметров и остаётся фиксированной на протяжении одного звонка; overlay показывает её настолько, насколько контекст ждёт второго участника, даже если она получила `sessionId` из внешней ссылки.
- `sessionId` нигде больше не отражается, чтобы избежать рассинхронизации, а сам URL остаётся видимым, пока активен overlay.

## Связи

- `ctx/docs/composition/client/ui/layout/skeleton.md` — наложение помещается над скелетом и не перестраивает его зоны.
- `ctx/docs/composition/client/ui/components/big-button.md`, `ctx/docs/composition/client/ui/components/screen-header.md`, `ctx/docs/composition/client/ui/components/screen-note.md` — визуальные атомы, обеспечивающие контролы и текст наложения.
- `ctx/docs/composition/client/ui/patterns/toast-layer.md` — статусы и ошибки, связанные с шарингом, выходят через тосты, а не через `screen-note`.
- `ctx/docs/composition/client/states/share-link.md` — состояние `waiting` привязывается к этому наложению.
- `ctx/docs/composition/client/ui/patterns/style.md` — тема, которой придерживается overlay, чтобы цвета и тени не отличались от основного интерфейса.
