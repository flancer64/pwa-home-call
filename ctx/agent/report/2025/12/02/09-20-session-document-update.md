## Резюме изменений
- Переименовал `ctx/product/capabilities/connection.md` в `ctx/product/capabilities/session.md` и обновил шапку документа.
- Перенастроил ссылки по всему `ctx/`, чтобы они указывали на новую локацию, включая отчёты, планы, ограничения и архитектурные заметки.
- Уточнил карту уровня в `ctx/product/capabilities/AGENTS.md`, сохранил сортировку и обновил роли, чтобы сессия упоминалась вместо `connection`.

## Детали работ
- Перенёс документ о жизненном цикле сеанса в `session.md`, сохранив декларативную структуру и обновив `Path`.
- Массивно заменил `capabilities/connection` → `capabilities/session` в документах, где фиксируется соответствие кода, ограничений и планов проекту, чтобы исключить несуществующие ссылки.
- Оптимизировал карту уровня `capabilities`, сохранив порядок каталога/файлов и описания, отражающие новую терминологию.

## Результаты
- Новое имя документа закреплено во всех контекстных ссылках, и карта уровня не вызывает несогласованности.
- Завершённая итерация зафиксирована в отчёте `ctx/agent/report/2025/12/02/09-20-session-document-update.md`.
