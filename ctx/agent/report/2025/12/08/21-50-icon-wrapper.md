# 21-50-icon-wrapper

Path: `./ctx/agent/report/2025/12/08/21-50-icon-wrapper.md`

## Резюме изменений
- Привёл `icon-wrapper` к спецификации `ctx/composition/client/ui/components/icon-wrapper.md`: компонент теперь принимает `name`/`size`, загружает SVG из `web/assets/icons`, не добавляет фон и ограничивает визуальные правила тематикой.
- Обновил шаблоны `home`, `call`, `settings` под новый API без вложенных `<img>`, чтобы все встраиваемые иконки рендерились единообразно.
- Повысил `web/version.json` до `20251208-214847`, сохранив инвариант обновлённой сборки при изменениях в `web/`.

## Детали работ
- Реализовал загрузку `web/assets/icons/<name>.svg` через `ICON_BASE`, нормализацию размеров `small/medium/large` и чистую вставку `<img>` без слотов в `web/ui/component/icon-wrapper.js` и сопутствующий CSS.
- Адаптировал макеты `web/ui/screen/home.html`, `web/ui/screen/call.html`, `web/ui/screen/settings.html` так, чтобы каждый `icon-wrapper` предоставлял `name`, а визуальные тюнинги (фон, тон) управлялись внешними стилями.
- Обновил `web/version.json` до `20251208-214847`, соблюдая условие о наличии версии в рамках правок `web/`.

## Результаты
- UI теперь отображает иконки исключительно через новый `icon-wrapper`, а все визуальные сигнализаторы остаются в рамках цветовых переменных темы.
- Версия клиентского слоя установлена в `20251208-214847`.
- Тесты не запускались (интерфейсные правки требуют ручной проверки).
