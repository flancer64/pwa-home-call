## Резюме изменений
- устранил падение установки PWA, вызванное попыткой добавить в кеш несуществующий модуль `app/Ui/Router/Dev.mjs`, и привёл список `CORE_ASSETS` к реальному набору файлов на `web/`.
- обновил `web/version.json` до `20251208-064928`, чтобы отражать новую сборку веб-слоя.

## Детали работ
- проверил список `CORE_ASSETS` в `web/service-worker.js` и обнаружил, что один из путей больше не существует, что мешало `cache.addAll` завершиться.
- удалил отсутствующий путь из перечня, чтобы установка шла без ошибок, и перепроверил список существующих файлов скриптом.
- зафиксировал новую версию сборки `20251208-064928` в `web/version.json` в соответствии с политикой `web/AGENTS.md`.
- прогнал `npm run test:unit`, чтобы убедиться, что регрессы не появились.

## Результаты
- `web/service-worker.js` теперь кеширует только существующие ресурсы, установка снова успешна.
- `web/version.json` содержит актуальную метку `20251208-064928`.
- `npm run test:unit` (пассы: 5, неудач: 0).
