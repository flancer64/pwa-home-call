# Отчёт итерации

## Цель
Синхронизировать документацию фронтенда HomeCall с новой моделью модульного монолита и отсечь зависимости через контейнер `@teqfw/di`.

## Действия
- Переписал `ctx/rules/arch/front.md`, закрепив семь контуров, схему взаимодействия (Core ↔ EventBus ↔ остальные) и роль позднего связывания.
- Добавил документы `ctx/rules/web/{core,env,ui,media,rtc,net,shared}.md`, описывающие назначение каждого контура, интерфейсы событий/колбэков и типичные DI-зависимости.
- Обновил `ctx/rules/architecture.md`, указав, что фронтенд состоит из контуров, управляется контейнером `@teqfw/di` и собирается агентами/LLM-агентами через позднее связывание.
- Включил ссылки на архитектурную документацию в `README.md`, чтобы обеспечить доступность карты контуров и зависимостей.

## Результаты
- Документы согласованы: контуры описаны декларативно, таблицы фиксируют ответственности, повторное использование контейнера подчёркнуто.
- Архитектура теперь ссылается на `ctx/rules/arch/front.md`, а `ctx/rules/architecture.md` описывает проект в крупномасштабном разрезе.
- Все изменения зафиксированы в ADSM-отчёте.

## Тесты
- Не выполнялись (документирование архитектуры).
