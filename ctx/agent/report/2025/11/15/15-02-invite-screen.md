## Резюме изменений
- Построил экран `invite` как `screen-card` с заголовком, блоком ссылки и двумя крупными кнопками, строго в соответствии с `ctx/rules/web/ui/screens/invite.md`.
- Обновил стили, шаблон и контроллер, чтобы сохранялся тёмный градиентный фон, зелёная палитра и компактный интерфейс без лишних инструкций.

## Детали работ
- Заменил `web/ui/invite.html` и `web/assets/style.css`, убрал подсказки/подзаголовки, реализовал ссылку в компактном тёмном блоке и добавил FAB-кнопку закрытия аналогично экрану настроек.
- Переписал `HomeCall_Web_Ui_Screen_Invite` в коде, чтобы динамически добавлять одну из двух кнопок (поделиться/копировать) вместе с «Начать звонок», привязал их к сервису `InviteService` и к остальным колбэкам, а `Flow` теперь передаёт `onClose` для возврата на `home`.
- Логика шаринга/копирования осталась в `InviteService`, которая обеспечивает fallback по `share-link.md`, а экран больше не выводит дополнительные подписи или подсказки.

## Результаты
- Экран приглашения доступен с минималистичным заголовком, ссылкой и двумя крупными действиями, включая функцию возвращения на главный экран и запуск звонка.
- Обновлён `web/version.json` → `20251115-150208`, чтобы веб-слой подтянул новую версию сборки.
