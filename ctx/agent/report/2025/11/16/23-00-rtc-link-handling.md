# Итерация 2025-11-16 23:00 — обработка разрывов RTC

## Цель
Уточнить инструкции в `ctx/rules/arch/rtc` по поведению при непреднамеренных разрывах и по явному завершению звонка.

## Действия
- Добавил в `signaling.md` описание `hangup`-сообщения, правило его отправки при нажатии кнопки «Завершить» и рестарт логики при случайных обрывах.
- Дополнил `media-flow.md` разделом о повторных попытках восстановления соединения и запрете автозавершения, пока пользователь не нажмёт кнопку.

## Результаты
Контекстные документы теперь явно требуют авто-подключения после непреднамеренных разрывов, а завершение разговора осуществляется только через явное `hangup` и соответствующую передачу по сигналингу.
