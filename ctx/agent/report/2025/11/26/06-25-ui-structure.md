# Iteration report – UI documentation

## Резюме изменений
- Уточнил инструкцию уровня UI, добавив описание каталога `screens/` и семь критериев качества документации ADSM.
- Перенёс документ `screens.md` внутрь `screens/` как `AGENTS.md`, сохранив контур `home → invite → call → end` и описания подсостояний.

## Детали работ
- Обновил `ctx/rules/web/ui/AGENTS.md`, чтобы он фиксировал открытую структуру каталога, подытоживал роль layout/notifications/screens/share-link/style и подчинялся критериям декларативности, полноты, непротиворечивости, связности, плотности, компактности и неизбыточности.
- Создал `ctx/rules/web/ui/screens/AGENTS.md` как индекс, связывающий state-файлы с переходами, статусами и механикой уведомлений.

## Результаты
- Документация UI-уровня теперь полно и непротиворечиво описывает ключевые файлы и контур экранов.
- `screens/` получила собственный AGENTS-файл, а все упомянутые документы второй строчкой указывают путь.
- Отчёт подтверждает связь между требованиями и изменениями контекста.
