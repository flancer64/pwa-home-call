# Iteration Report — Call Button Position

## Goal
- Place the "Завершить звонок" CTA in the call view at the bottom-left corner instead of centered.

## Actions
- Updated `web/assets/style.css` so `.call-controls` is anchored to the bottom-left inset with left padding and the button aligns to the start.

## Results
- The call screen now positions the end-call FAB in the lower-left, meeting the requested layout change.
