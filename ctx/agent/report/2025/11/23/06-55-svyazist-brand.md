# Резюме изменений
- переименованы упоминания Kolobok на Svyazist в `README.md`, documentation strings серверной части и логах, чтобы отражать ребрендинг;
- обновлены утилиты и юнит-тесты в `src/` и `test/` (ссылки, ключи кеша, строки логов) для согласованной работы с новым названием.

# Детали работ
1. Используя инструкции из `AGENTS.md` и `ctx/AGENTS.md`, сконцентрировался на разрешённых каталогах и аккуратно заменил `Kolobok/kolobok/KOLOBOK` на `Svyazist/svyazist/SVYAZIST` без затрагивания `@LLM-DOC` комментариев.
2. Обновил `README.md` в рамках требований ребрендинга, а также читаемость backend описаний (`src/Back/App.js`, `src/Back/Logger.js`, `src/Back/Service/Signal/Server.js`).
3. Привёл тесты и сопроводительные строки в `test/` к одному бренду: адаптировал URL, ключи кеша и проверяемые сообщения, чтобы автоматические контролы работали с `svyazist`.

# Результаты
- проекта больше не содержит упоминаний старого бренда в указанной области (`README.md`, `src/`, `test/`);
- логика и тесты по-прежнему ссылаются на `HomeCall_*` пространства имён, но текстовые метки отражают новый бренд;
- отчёт за последнюю итерацию зафиксирован в `ctx/agent/report/2025/11/23/06-55-svyazist-brand.md`.
