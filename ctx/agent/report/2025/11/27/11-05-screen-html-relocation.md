## Резюме изменений
- переместил все HTML-шаблоны экранов (`home`, `invite`, `call`, `end`, `settings`) из `web/ui/` в `web/ui/screen/`, как требует `ctx/rules/web/structure.md`
- обновил сервис-воркер и загрузчик шаблонов так, чтобы они запрашивали файлы по новым путям `ui/screen/<screen>.html`
- поднял `web/version.json` до `20251127-110504` для соблюдения требования обновления версии при изменениях в `web/`

## Детали работ
- создал каталог `web/ui/screen` и перенёс туда все screen-шаблоны, оставив в `web/ui` только CSS и компоненты
- адаптировал `web/service-worker.js` и `web/app/Ui/Templates/Loader.mjs`, заменив строки `'ui/<screen>.html'` на `'ui/screen/<screen>.html'` в списках кеширования и загрузки
- обновил единственное поле `version` в `web/version.json`, чтобы сервисный воркер читал новый UTC-таймстамп

## Результаты
- структура `web/ui` теперь соответствует карте из `ctx/rules/web/structure.md` и все HTML-экраны находятся в `web/ui/screen`
- кеширование, загрузка шаблонов и PWA-обновления работают с актуальными путями `ui/screen/<screen>.html`
- новая версия `20251127-110504` отражена в `web/version.json`, позволяя клиентам получить свежую сборку
