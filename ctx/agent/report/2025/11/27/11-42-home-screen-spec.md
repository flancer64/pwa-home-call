# Home Screen Spec Alignment

## Резюме изменений
- Привёл разметку `home`-экрана в соответствие с `ctx/rules/web/ui/screen/home.md`, добавив заголовок «Приложение», действие настроек и описанный CTA.
- Настроил обработку новой кнопки: `HomeCall_Web_Ui_Screen_Home` прокидывает `onOpenSettings`, а контроллер потока запускает экран настроек.
- Обновил `web/version.json` → `20251127-114243` для корректной работы PWA-обновлений.

## Детали работ
- Проверил наличие `header/action/main/footer` и SLA-компонентов из документации, убедился, что `screen-card` и `screen-note` используются как указано.
- В `web/ui/screen/home.html` зафиксировал `screen-header` с заголовком и кнопкой настроек (`action`-слот) и добавил атрибут `icon="link"` к `big-button`.
- Обновил `web/app/Ui/Screen/Home.mjs` и `web/app/Ui/Flow.mjs`, чтобы новая кнопка вызывала `showSettings` через существующий UI-контроллер, возвращая пользователя обратно на `home` после закрытия.
- Отразил изменения в версии `web/version.json` и подтвердил, что текущая сборка теперь требует `20251127-114243`.

## Результаты
- Домашний экран строго соответствует спецификации `ctx/rules/web/ui/screen/home.md`, включая вызов `settings`.
- `web/version.json` синхронизирован с изменениями интерфейса и готов к доставке новой сборки.
