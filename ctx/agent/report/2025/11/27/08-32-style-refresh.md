## Резюме изменений
- Привёл глобальные переменные и компонентные стили в соответствие с `ctx/rules/web/ui/style.md`, добавив темы, градиенты и минимальные состояния, описанные в документе.
- Вынес оформление тостов в отдельный `ui/toast.css`, подключил его в `web/index.html` и добавил в кеш `web/service-worker.js`.
- Обновил версию сборки `web/version.json` → `20251127-083210`, чтобы PWA-обновление отражало визуальные изменения.

## Детали работ
- Расширил `web/ui/ui.css`: добавил тему (`--accent-gradient`, `--signal-*`, `--toast-*`, `--panel-gradient` и прочие переменные), убрал локальные цвета тостов и оставил только базовую структуру Shell.
- Создал `web/ui/toast.css` с контейнером и вариантами цветов `info`, `success`, `warn`, `error`, которые используют переменные темы и точные тени по гайду.
- Адаптировал `big-button`, `icon-wrapper` и `screen-card` к новым переменным (градиенты, фоны, цвета текста, тени, контуры) и соблюдению требуемого размера/ритма.
- Добавил ссылку на `ui/toast.css` в `web/index.html` и включил файл в `STATIC_ASSETS` сервис-воркера для надежного кеширования.
- Версию билд-метаданных сохранил как `20251127-083210`, как того требует правило `web/AGENTS.md`.

## Результаты
- Интерфейс теперь использует единую палитру, крупные зоны касания и минимальный `hover`, как описано в `ctx/rules/web/ui/style.md`.
- Стиль тостов отделён в собственный файл, сохраняя единство компонентного слоя и прозрачность PWA-кеша.
- Пользователи получат новую сборку `svyazist-v20251127-083210`, что подтверждено версией и отчётом.
