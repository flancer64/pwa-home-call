## Резюме изменений
- Привёл `screen-end` к единым трём зонам без дублирующего текста: в хедере только `kicker` и заголовок, а `#end-message` теперь коротко поясняет ход выполнения в `hint`.
- Обновил стили CTA и градиента, чтобы фон, кнопка и размер иконки совпадали с домашним экраном, и убедился, что зона действия центрирована как в `home`.
- Синхронизировал версию PWA, потому что изменения затрагивают Web-контент.

## Детали работ
- Удалил второй подзаголовок из хедера, переставил `#end-message` в `hint` и заменил текст на «Можно начать новый сеанс в любой момент.» для лаконичного пояснения.
- Добавил `.end-action-button` с тем же градиентом, высотой, тенью и выравниванием, что и кнопка «Связать», зафиксировал размеры иконки и текст кнопки, обеспечил центровку зоны и базовую линию подсказки.
- Перенастроил фон `screen-end` на градиент `#002d2a → #0e6251`, выровнял параметры `hint` и `screen-note`, сохранил первоначальный DOM и все существующие `id`.
- Обновил `web/version.json` → `20251127-060326`, чтобы PWA обновил сборку.

## Результаты
- Экран End визуально согласован с Home и сохраняет прежнюю функциональность (контролы, `return-home`, `sessionId`).
- `web/version.json` теперь содержит `20251127-060326`.
- `npm run test:unit` (fails: `test/unit/Back/Service/Signal/Server.test.mjs` — `test failed`).
