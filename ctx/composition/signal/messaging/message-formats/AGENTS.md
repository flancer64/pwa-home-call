# JSON-форматы

Путь: `ctx/composition/signal/messaging/message-formats/AGENTS.md`

## Назначение

Каталог содержит конкретные JSON-сообщения, которые пересылаются через `wss://<host>/signal`.

## Карта уровня

- `AGENTS.md` — текущий указатель.
- `websocket.md` — таблица типов и полей (`offer`, `answer`, `candidate`, `hangup`, `error`).
