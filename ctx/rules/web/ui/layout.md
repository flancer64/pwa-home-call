# Архитектура экранов и панелей управления

## Назначение

Документ определяет принципы **композиции интерфейса** браузерного приложения HomeCall:  
структуру экранов, расположение элементов, использование иконок и унификацию панелей управления.  
Цель — обеспечить единообразие UX при минимальном когнитивном усилии пользователя.

---

## 1. Общая композиция

- Все экраны используют **общий каркас**:
  - верхняя панель (toolbar) для глобальных действий,
  - основная область для контентных элементов,
  - нижняя зона для CTA-кнопок (действий, связанных с текущим экраном).
- Toolbar фиксирован, доступен на всех состояниях (`enter`, `lobby`, `call`, `end`).
- Основная область изменяется при смене состояния, но не разрушает общую вёрстку.

---

## 2. Верхняя панель (toolbar)

### Назначение

Единая точка доступа к функциям, не связанным с самим вызовом.

### Состав

| Элемент            | Назначение                                          |
| ------------------ | --------------------------------------------------- |
| 🗑 **Очистить кеш** | сбрасывает кэш PWA (service worker + localStorage); |
| 🎛 **Устройства**   | включает/отключает камеру и микрофон;               |
| 🔄 **Обновить**    | перезагружает приложение без выхода из PWA.         |

### Принципы

- Только иконки, подписи — во всплывающих подсказках.
- Панель не перекрывает видеопотоки и уведомления.
- На мобильных устройствах — схлопывается в кнопку-меню.

---

## 3. Основная область экранов

| Состояние | Содержимое                                          | Особенности                                    |
| --------- | --------------------------------------------------- | ---------------------------------------------- |
| **enter** | поля ввода имени и кода комнаты; CTA-кнопка «Войти» | крупные элементы, текст-объяснение 3–4 строки  |
| **lobby** | список собеседников; кнопка «Позвонить»             | крупные карточки, минимальный текст            |
| **call**  | поток собеседника + миниатюра пользователя          | минимальное количество кнопок, акцент на видео |
| **end**   | сообщение «Вызов завершён»; CTA «Вернуться»         | центрированный текст, CTA внизу                |

---

## 4. Нижняя зона (CTA-панель)

- Находится внизу экрана, занимает полосу 10–15 % высоты.
- Содержит основное действие текущего состояния (например, «Начать звонок», «Разрешить доступ»).
- Цвет CTA-кнопки соответствует акцентному цвету из `style.md` (`#0078d7`).
- Панель скрывается в режиме звонка, если отображается видео в полноэкранном виде.

---

## 5. Иконография и обозначения

- Все действия обозначаются пиктограммами из набора SVG-иконок в `assets/icons/`.
- Размер — 24 px на десктопе, 32 px на мобильных.
- Цветовая кодировка:
  - синий — активное состояние;
  - серый — доступно;
  - красный — ошибка или выключение устройства.

---

## 6. Адаптивность

- Toolbar и CTA-панель фиксированы, масштабируются пропорционально.
- Основная область растягивается по высоте и центрируется.
- Миниатюры видео и кнопки не перекрывают подсказки.
- В альбомной ориентации toolbar может перемещаться в левый край.

### Ограничение ширины и выравнивание панелей

- **Малые дисплеи (до 768 px)** — информационные блоки и формы занимают всю ширину окна, без боковых отступов.  
  Это повышает читаемость и не тратит ограниченное пространство экрана.
- **Средние и большие дисплеи (от 768 px)** — панели и формы центрируются на странице, ограничиваются `max-width: 640px`.  
  Текст и кнопки выравниваются по центру, чтобы избежать растяжения интерфейса и сохранить визуальный фокус.
- Центровка и ограничение ширины не влияют на расположение toolbar и CTA-панели: они всегда тянутся по ширине окна.

---

## 7. Связи

- `./screens.md` — сценарии экранов и переходов.
- `../style.md` — визуальные принципы и адаптивность.
- `../app.md` — сценарное поведение состояний приложения.
- `../arch/front.md` — архитектура фронтенда.
